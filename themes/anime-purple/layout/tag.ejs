<!-- Tag Hero Section -->
<section class="page-hero" <% if (theme.images && theme.images.page_hero_bg) { %>style="background-image: url('<%- config.root %><%- theme.images.page_hero_bg.replace(/^\//, '') %>'); background-size: cover; background-position: center;"<% } %>>
    <div class="page-hero-content">
        <h1 class="page-hero-title"><i class="fas fa-tag"></i> <%- page.tag %></h1>
        <p class="page-hero-desc">共 <%- site.tags.findOne({name: page.tag}).posts.length %> 篇文章</p>
        <div class="category-hero-breadcrumb">
            <a href="<%- config.root %>"><i class="fas fa-home"></i> 首页</a>
            <span class="breadcrumb-sep"><i class="fas fa-chevron-right"></i></span>
            <a href="<%- config.root %>tags/">标签</a>
            <span class="breadcrumb-sep"><i class="fas fa-chevron-right"></i></span>
            <span class="breadcrumb-current"><%- page.tag %></span>
        </div>
    </div>
</section>

<div class="container">
    <!-- All Tags Cloud -->
    <section class="tags-index" style="padding-top: var(--spacing-3xl);">
        <h2 class="section-title">标签云</h2>
        <div class="tags-cloud-full">
            <% site.tags.sort('name').forEach(function(tag) { %>
                <a href="<%- config.root %><%- tag.path %>" class="tag-cloud-card <%- tag.name === page.tag ? 'active' : '' %>">
                    <span class="tag-cloud-card-name"># <%- tag.name %></span>
                    <span class="tag-cloud-card-count"><%- tag.posts.length %> 篇</span>
                </a>
            <% }); %>
        </div>
    </section>

    <!-- Posts in Current Tag -->
    <section class="cat-posts">
        <h2 class="section-title"><%- page.tag %> · 文章列表</h2>
        <div class="cat-posts-grid">
            <%
            var posts = site.tags.findOne({name: page.tag}).posts.sort('-date');
            posts.forEach(function(post) {
            %>
                <article class="post-card">
                    <% if (post.cover) { %>
                        <div class="post-cover">
                            <img src="<%- post.cover %>" alt="<%- post.title %>">
                        </div>
                    <% } %>
                    <div class="post-card-content">
                        <h3 class="post-title">
                            <a href="<%- config.root %><%- post.path %>"><%- post.title %></a>
                        </h3>
                        <p class="post-excerpt"><%- post.excerpt || strip_html(post.content).substring(0, 150) %></p>
                        <div class="post-meta">
                            <span class="post-date">
                                <i class="fas fa-calendar"></i>
                                <%- date(post.date, 'YYYY-MM-DD') %>
                            </span>
                            <span class="post-category">
                                <i class="fas fa-folder"></i>
                                <%- post.categories.map(function(c){ return c.name }).join(' / ') %>
                            </span>
                        </div>
                        <div class="post-tags">
                            <% post.tags.forEach(function(tag) { %>
                                <a href="<%- config.root %><%- tag.path %>" class="tag"><%- tag.name %></a>
                            <% }); %>
                        </div>
                    </div>
                </article>
            <% }); %>
        </div>
    </section>
</div>
